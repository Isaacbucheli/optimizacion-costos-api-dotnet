{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },

  "_comment": "Stack NUEVO: el backend .NET usa SU PROPIA base sqldb-optimizacion-costos-valida (clon de prod), NUNCA la de prod (sqldb-optimizacion-costos, que es de Python). SQL_USERNAME/SQL_PASSWORD y secretos van por variables de entorno / user-secrets, no aquí.",
  "SQL_SERVER": "sqldb-optimizacion-costos.database.windows.net",
  "SQL_DATABASE": "sqldb-optimizacion-costos-valida"
}
